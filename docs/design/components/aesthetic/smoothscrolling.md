# Smooth Scrolling

## 记录

- 类型：mod
- 状态：active
- 证据可信度：verified
- 来源路径：`mods/smoothscrolling-1.21.1-NeoForge-1.0.1.jar`
- 标识与版本：smoothscrolling 1.0.1
- 加载器或包格式：NeoForge 21.1.235（Minecraft 1.21.1）

## 设计作用

Smooth Scrolling 为 ProjectNautic 提供快捷栏、聊天栏、创造模式物品栏及各类条目列表的平滑滚动，直接服务于"UI 愉悦感"设计支柱。它将原版瞬跳的滚动替换为带惯性的平滑滚动，使玩家在切换物品、浏览聊天或翻页时感觉更流畅。

该模组与 Smooth GUI 共同构成 ProjectNautic 的界面动效层：Smooth Scrolling 负责列表与快捷栏滚动，Smooth GUI 负责屏幕整体出现/消失动画。

## 玩家可见行为

- **可发现性**：玩家滚动鼠标滚轮切换快捷栏、浏览聊天历史、翻创造模式物品栏或各类配置列表时，内容会以平滑动画方式移动。
- **交互**：无需主动操作；玩家可在配置文件中调整各区域的平滑度与速度。
- **进度/奖励**：无内置进度；平滑滚动作为日常操作的视觉润色。
- **摩擦/失败**：平滑度过高可能让玩家感觉操作有延迟或"粘滞"；某些模组的自定义列表可能不被支持。

## 集成关系

- **依赖**：Minecraft 1.21.1（required, CLIENT）。
- **冲突**：无已知严重冲突。
- **协作**：与 **Smooth GUI** 共同强化界面动效。
- **配方/标签**：无直接参与。
- **任务**：无。
- **世界生成**：无。
- **UI**：全局影响快捷栏、聊天、创造模式物品栏、条目列表等滚动区域。
- **资源**：依赖内置动画逻辑，无需额外资源包。
- **脚本**：无直接 KubeJS 交互。
- **加载顺序假设**：标准 CLIENT 端加载。

## 配置意图

`config/smoothscrolling-client.toml` 当前配置对各滚动区域做了差异化平滑度设置：

- **Hotbar**：`smoothness=0.1`，`rollover=true`。快捷栏滚动平滑度调至较低（0.5 → 0.1），优先保证切换武器/工具时的响应速度；同时启用滚轮越过首尾时循环滚动，提升操作效率。
- **Chat**：`smoothness=0.5`，`openingSmoothness=0.5`。聊天栏滚动与打开动画均采用中等平滑度，兼顾流畅感与可读性。
- **Creative Screen**：`smoothness=0.5`。创造模式物品栏滚动采用中等平滑度，避免翻页过快导致找不到物品。
- **Entry List**：`smoothness=0.5`，`speed=30.0`。条目列表（如配置界面、模组列表）滚动中等平滑，自动速度为 30 px，保持适中响应。
- **Misc**：`enableMaskDebug=false`，关闭遮罩调试可视化。

整体配置意图是"快捷栏求快、列表求稳"：对需要快速反应的快捷栏降低平滑度，对需要精确浏览的聊天/创造/列表提高平滑度。

## 兼容性与性能

- **客户端/服务端范围**：仅 **CLIENT**。
- **存档影响**：无。
- **已知不兼容**：无。
- **资源成本**：纯 CLIENT 端 UI 动画，开销极低。
- **缓解措施**：通过分区域平滑度控制，避免所有滚动都产生粘滞感。

## 验证

- [ ] 启动或加载测试
- [ ] 功能测试
- [ ] 多人游戏测试（如适用）
- [ ] 性能或世界生成测试（如适用）
- [ ] 相关系统的回归检查

## 风险与开放问题

1. 与 Modern UI 的视图系统或自定义滚动组件叠加时，可能出现平滑度重复计算或滚动错位。
2. 部分模组自定义的条目列表可能无法被 Smooth Scrolling 识别，导致体验不一致。

## 历史

- 2026-07-21: 快捷栏 `smoothness` 0.1 → 0.0、`rollover` true → false；聊天 `smoothness` 0.5 → 0.0、`openingSmoothness` 0.5 → 0.1（进一步收敛滚动动画，优先操作响应；与 GUI Tween 引入的动画层并存时降低叠加噪音）
- 2026-07-20: 快捷栏滚动平滑度 `smoothness` 0.5 → 0.1（回调：优先操作响应速度，0.5 手感偏钝）
- 2026-07-19: 快捷栏滚动平滑度 `smoothness` 0.2 → 0.5（提升滚动物理感，与聊天/列表平滑度对齐）
- 2026-07-16: 作为第三批 QoL 模组添加
